function myFunction(){
	document.getElementById("hyperlink").innerHTML = add();

    var add = (function () {
        var counter = 0;
        return function () {return counter += 1;
        
    }
    const newLocal = function () {
		var counter = 0;
		return function () {
			return counter +

            let link = document.getElementById("hyperlink");
// declare a variable called link and set it equal to the element in the HTML document with the ID of "hyperlink"
let count = 0;
// declare a variable and set it to 0
document.body.addEventListener("click", hyperLink)
// make sure the whole entire body of the html document is listening for someone to click on it, when someone does click on it, call the function called "alternate-version"
{;
	let count = 0
	let message = document.getElementById("hyperlink");
	
	document.body.addEventListener("click",hyperlink)
	var clicks = 0;

function onClick() {
  clicks += 1;
  document.getElementById("clicks").innerHTML = clicks;
};
var clicks = 0;
    function myFunction() {

        clicks += 1;
        document.getElementById("hyperlink").innerHTML = clicks;


    }let hyperLink = document.querySelector("hyperLink");
	let CounthyperLinkClicks = 0;
	
	hyperLink.addEventListener("click", function() {
	  CounthyperLinkClicks += 1;
	  console.log(CounthyperLinkClicks);
	});
	function hyperLink() {
		count++
		
		}
	}